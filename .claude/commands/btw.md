# /btw — Context Injection
Pass context to the current task without polluting the main thread.

Usage: /btw [context]
Example: /btw I just changed the schema in db.ts — Recipe type now has a prefermentType field

What to do:
1. Acknowledge the context in one sentence
2. Note if it affects the current task
3. Continue current task without restarting
